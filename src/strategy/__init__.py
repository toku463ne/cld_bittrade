"""Strategy definitions.

Each strategy inherits from :class:`src.strategy.base.Strategy` and is registered
in :mod:`src.strategy.registry`. Strategies are strategy-agnostic to the rest of
the framework: the simulator only depends on the base interface.
"""
