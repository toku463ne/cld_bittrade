"""One-off analysis / probe scripts.

Per the sign-debate protocol, the judge's autonomous "next action" may write a
small (<= ~250 line) probe here mirroring an existing script's structure. See
``.claude/commands/sign-debate.md``.
"""
