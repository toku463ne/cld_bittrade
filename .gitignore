# Environments (never commit real credentials)
.env
.env.dev
.env.bt
.env.prod
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# uv: commit uv.lock for reproducible app builds (not ignored).

# Claude Code local (personal) settings — shared agents/commands ARE committed.
.claude/settings.local.json
# Transient harness state (scheduler locks etc.)
.claude/scheduled_tasks.lock

# Data / logs (anchored to repo root so they don't match src/data, src/logs, ...)
*.log
/logs/
/data/
*.sqlite

# OS
.DS_Store
.viz_cache/
